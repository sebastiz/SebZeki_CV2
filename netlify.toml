[build]
  command = "pip install requests --quiet && python scripts/build_from_content.py && hugo --gc --minify -b $URL"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.72.0"
  HUGO_ENABLEGITINFO = "true"
  PYTHON_VERSION = "3.11"

[context.production.environment]
  HUGO_ENV = "production"

[context.deploy-preview]
  command = "pip install requests --quiet && python scripts/build_from_content.py && hugo --gc --minify --buildFuture -b $DEPLOY_PRIME_URL"

[context.branch-deploy]
  command = "pip install requests --quiet && python scripts/build_from_content.py && hugo --gc --minify -b $DEPLOY_PRIME_URL"

[[headers]]
  for = "*.webmanifest"
  [headers.values]
    Content-Type = "application/manifest+json; charset=UTF-8"

[[headers]]
  for = "index.xml"
  [headers.values]
    Content-Type = "application/rss+xml"
